{
  "installCommand": "npm cache clean --force && rm -rf node_modules package-lock.json && npm install && npm install --package-lock-only",
  "buildCommand": "npm run build:vercel",
  "framework": "vite",
  "outputDirectory": "dist"
} 